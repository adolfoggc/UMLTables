<%= form_with(model: my_project, local: true) do |form| %>
  <% if my_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(my_project.errors.count, "error") %> prohibited this my_project from being saved:</h2>

      <ul>
      <% my_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
